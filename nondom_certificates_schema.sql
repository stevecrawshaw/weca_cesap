CREATE OR REPLACE TABLE nondom_certificates (
    LMK_KEY VARCHAR,
    ADDRESS1 VARCHAR,
    ADDRESS2 VARCHAR,
    ADDRESS3 VARCHAR,
    POSTCODE VARCHAR,
    BUILDING_REFERENCE_NUMBER VARCHAR,
    ASSET_RATING INTEGER,
    ASSET_RATING_BAND VARCHAR,
    PROPERTY_TYPE VARCHAR,
    INSPECTION_DATE DATE,
    LOCAL_AUTHORITY VARCHAR,
    CONSTITUENCY VARCHAR,
    COUNTY VARCHAR,
    LODGEMENT_DATE DATE,
    TRANSACTION_TYPE VARCHAR,
    NEW_BUILD_BENCHMARK VARCHAR,
    EXISTING_STOCK_BENCHMARK VARCHAR,
    BUILDING_LEVEL VARCHAR,
    MAIN_HEATING_FUEL VARCHAR,
    OTHER_FUEL_DESC VARCHAR,
    SPECIAL_ENERGY_USES VARCHAR,
    RENEWABLE_SOURCES VARCHAR,
    FLOOR_AREA INTEGER,
    STANDARD_EMISSIONS DECIMAL(10,2),
    TARGET_EMISSIONS DECIMAL(10,2),
    TYPICAL_EMISSIONS DECIMAL(10,2),
    BUILDING_EMISSIONS DECIMAL(10,2),
    AIRCON_PRESENT VARCHAR,
    AIRCON_KW_RATING DECIMAL(10,2),
    ESTIMATED_AIRCON_KW_RATING DECIMAL(10,2),
    AC_INSPECTION_COMMISSIONED INTEGER,
    BUILDING_ENVIRONMENT VARCHAR,
    "ADDRESS" VARCHAR,  -- Changed from ADDRESS
    LOCAL_AUTHORITY_LABEL VARCHAR,
    CONSTITUENCY_LABEL VARCHAR,
    POSTTOWN VARCHAR,
    LODGEMENT_DATETIME VARCHAR,
    PRIMARY_ENERGY_VALUE INTEGER,
    UPRN BIGINT,
    UPRN_SOURCE VARCHAR,
    "filename" VARCHAR
);